void setup() {
  // 입력은 0 으로 값을준다 (출력 1을 줘야지 출력이 나간다)
DDRB = B00000000 ;
DDRD = B10000000 ;
Serial.begin(9600) ;
}

void loop() {
  // 한번 실행하고 나서 끊어주기 전까지 무한 반복한다. (입력에서 출력을 주면 여기서는 출력 여부를 결정한다) 이 회로는 출력에 1을 넣으면 전원이 OFF 0을 넣으면 ON
Serial.println(PINB) ;
PORTD = B00000000 ;
if(PINB == 200)
{
 PORTD = B00000000 ;
}
else
{
 PORTD = B10000000 ;
}
}
//객관식과 주관식은 없고 괄호넣기만 시험에 출제했다.
