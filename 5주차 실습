void setup() {
  // 여기는 방향을 설정하는 란 이라서 사용하는 핀에 따라서 전부 단락시킨다.
 DDRD = B11111110 ; 
 PORTD = B00000000 ;
}

void loop() {
  // 여기는 주변기기에 출력으로 나가는 란 이다 키거나 원하는 동작은 여기서 작성하면된다.
 PORTD = B01100110 ;
 delay(1000) ;

 PORTD = B00111110 ;
 delay(1000) ;
}
